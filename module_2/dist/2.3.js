"use strict";
{
    // const rollNumbers:number[]=[368];
    // its genaric types
    const rollNumbers = [368];
    const mentors = ['kakku ', 'messi'];
    const boolArray = [true, false, true, false];
    const user = [
        {
            name: 'kakku2',
            age: 18,
        },
        {
            name: 'kakku',
            age: 26,
        }
    ];
    const add = (x, y) => x + y;
    add(20, 30);
    const manus = ['MR. Moon', 'Aziza'];
    const userWithId = [1234, {
            name: 'parsing',
            email: 'parsing@gmail.com'
        }];
}
